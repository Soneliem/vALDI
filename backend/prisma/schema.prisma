generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id     String   @id @unique
  tokens String[] @default([])
  client Json     @default("{}")
  skins  String[] @default([])
}
