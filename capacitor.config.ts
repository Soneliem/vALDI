import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "dev.sonel.valdi",
  appName: "vALDI",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
