{
  "name": "v-aldi",
  "description": "A Store Checker for Valorant",
  "private": true,
  "scripts": {
    "back:start": "yarn --cwd backend start",
    "front:start": "yarn --cwd frontend start",
    "start": "npm-run-all --parallel back:start front:start"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
